//mixin son estrucutuas que tienen forma de fucncion con nombre y parametros

mixin itemMenu(item, index)
    li
        if (index == page -1)
             a.activo(href="#"+item.id.toLowerCase()+".html" tittle=item.description) #{item.nombre}
        else
        a(href=item.id.toLowerCase()+".html" tittle=item.description) #{item.nombre}
      

mixin inputGroup (inputName)
    .formGroup
        label(for=inputName.toLowerCase()) #{inputName}
        block
        p.error(hidden) Aquí irán los errores.