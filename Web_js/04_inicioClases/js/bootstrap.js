//fichero loader bootstrap o cargador 
import{ App } from './app.js'
document.addEventListener('DOMContentLoaded', () => {new App()} ) // tengo que instanciar la clase app //trabajar como el dos y el tres 